#usda 1.0
(
    defaultPrim = "World"
    metersPerUnit = 1.0
    upAxis = "Z"
)

def Xform "World"
{
    def "environment" (
        prepend references = @./simple_room.usd@
    )
    {
    }

    def "robot" (
        prepend references = @../robots/humanoid_with_camera.usd@
    )
    {
        float3 xformOp:translate = (0, 0, 0.1)
        uniform token[] xformOpOrder = ["xformOp:translate"]
    }
}
